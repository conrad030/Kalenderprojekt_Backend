var express = require("express");
var router = express.Router();

// User signup
router.post("/signup", (res, req) => {});

// User login
router.post("/login", (res, req) => {});

//TODO: Missing CRUDs etc.
